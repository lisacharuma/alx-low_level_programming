#include <stdio.h>
#include "main.h"

/**
 * main - check the code
 * return : 0
 */

void print_alphabet_x10(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}
