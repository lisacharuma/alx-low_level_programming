#include "main.h"

/**
 * _islower - takes parameter int c
 * Description - checks whether a given parameter is lower or not
 * Return: 1 if letter is lowercase and  0 if otherwise
 */

int _islower(int c)
{
	ch c;

	if (c >= 'a' && c <= 'z')
	{
		return (1);
	else
		return (0);
}
