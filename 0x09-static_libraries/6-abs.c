#include "main.h"

/**
 * _abs - entry point
 * Description - print absolute value
 * @n: integer of Abs value
 * Return: int n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
